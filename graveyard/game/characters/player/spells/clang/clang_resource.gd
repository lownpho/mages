extends SpellResource
class_name ClangResource

@export_group("Clang")
## Seconds the pierce buff lasts. While active, every weapon-spell bullet the
## caster fires passes through hurtboxes instead of despawning on the first hit.
@export var duration: float = 5.0
